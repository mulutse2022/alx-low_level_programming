#include "main.h"
#include <stdio.h>

/**
 * mul - a function which multiplies two integers
 * @a: input number one
 * @b: input number two
 * Return: returns multiplied result of the two numbers
 */
int mul(int a, int b)
{
return (a * b);
}
